matrix:
  fast_finish: true

build_script:
  - git submodule update --init --recursive
  - cmake --version
  - mkdir build
  - cd build && cmake -G "Visual Studio 15 2017" ..
  - cmake --build . --config Release
